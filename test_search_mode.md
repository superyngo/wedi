# 搜尋模式測試文件

## 測試場景

### 場景 1: 基本搜尋流程
1. 按 Ctrl+F 開啟搜尋
2. 輸入搜尋詞 "test"
3. 顯示匹配結果，進入搜尋模式
4. 按 PageDown 或 Ctrl+N → 跳到下一個 "test"
5. 按 PageUp 或 Ctrl+P → 跳到上一個 "test"

### 場景 2: ESC 退出搜尋模式
1. 在搜尋模式中按 ESC
2. 搜尋模式關閉（但保留搜尋結果）
3. 按 PageDown → 正常翻頁（不跳搜尋詞）
4. 按 PageUp → 正常翻頁（不跳搜尋詞）

### 場景 3: 重新進入搜尋模式
1. 按 Ctrl+F 重新開啟搜尋
2. 上次的搜尋詞自動填入
3. 按 Enter 重新啟用搜尋模式
4. PageDown/PageUp 又會跳搜尋詞

### 場景 4: 新搜尋覆蓋舊搜尋
1. 按 Ctrl+F 開啟搜尋
2. 輸入新的搜尋詞 "new"
3. 舊的搜尋結果被清除
4. 新的搜尋結果生效

## 測試詞彙

這裡有一些 test 詞彙可以搜尋。
這行也有 test 可以找到。
Test 大小寫也算是 test 嗎？
最後一個 test 在這裡。
